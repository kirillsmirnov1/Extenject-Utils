Utils to be used with [Zenject](https://github.com/modesttree/Zenject).

## Extensions

`T InjectBy<T>(this T obj,  DiContainer container)` injects dependencies into object and returns same object back.